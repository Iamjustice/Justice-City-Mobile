import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.justicecity.app',
  appName: 'Justice City',
  webDir: 'dist/public'
};

export default config;
